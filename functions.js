// a good function takes an input and gives an output, without modifying the input

function sayhi(name, name2) {
	return "Hi " + name + " and " + name2
}

console.log(sayhi("Zach", "Michael"))

// for-each function

function forEach(collection, func) {
	
}